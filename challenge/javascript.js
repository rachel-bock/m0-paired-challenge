// Because I will be writing in JavaScript, I chose the name of the
// language as the file name.

// String.
var title;

// String
var author;

// Could be a string or a number.
var copyrightDate;

// Number variable, lists number of pages in book.
var numberOfPages;

// Boolean type, lists whether or not the book is available to checkout.
var checkedOut;

// String type, stores name where book was checked out.
var nameOfLibrary;

// Patrons is to record who checked out the book.  Will store an array
// of strings with patron names.
var patrons;
